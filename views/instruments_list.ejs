<p>A list of all the single instruments present in the database</p>

<% if (inst_list.length) { %>
<ul>
	<% inst_list.forEach(function (elem) { %>
	<li><a href="<%= elem.url %>"><%= elem.name %></a></li>
	<% }) %>
</ul>
<% } %>
