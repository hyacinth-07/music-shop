<p>A list of all the types of instruments present in the database</p>

<% if (type_list.length) { %>
<ul>
	<% type_list.forEach(function (elem) { %>
	<li><a href="<%= elem.url %>"><%= elem.name %></a></li>
	<% }) %>
</ul>
<% } %>
